## General Information

The traversable `jamf_Update_Recurring_Scripts` edge represents a code execution path where the source has 'Update
Scripts' permission and there are scripts configured to run repeatedly on target computers via enabled policies. The
source can modify these scripts to execute arbitrary code on the in-scope computers at the next policy execution cycle.
